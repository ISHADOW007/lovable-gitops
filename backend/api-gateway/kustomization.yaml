apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - servicemonitor.yaml
  - canary-service.yaml
  - hpa.yaml
  - rollout.yaml
  

images:
  - name: satyamgrao007/lovable-api-gateway
    newTag: "1"
