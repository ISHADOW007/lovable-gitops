apiVersion: v1
kind: Service

metadata:
  name: api-gateway-canary
  namespace: lovable-core

spec:
  selector:
    app: api-gateway

  ports:
    - name: http
      port: 80
      targetPort: 8080

  type: ClusterIP