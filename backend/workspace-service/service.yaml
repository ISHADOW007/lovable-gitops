apiVersion: v1
kind: Service

metadata:
  name: workspace-service
  namespace: lovable-core
  labels:
    app: workspace-service

spec:
  selector:
    app: workspace-service

  ports:
    - name: http
      port: 80
      targetPort: 9020