apiVersion: v1
kind: Service

metadata:
  name: intelligence-service
  namespace: lovable-core
  labels:
    app: intelligence-service

spec:
  selector:
    app: intelligence-service

  ports:
    - name: http
      port: 80
      targetPort: 9030