apiVersion: v1
kind: Service

metadata:
  name: config-service
  namespace: lovable-core
  labels:
    app: config-service

spec:
  selector:
    app: config-service

  ports:
    - name: http
      port: 8888
      targetPort: 8888