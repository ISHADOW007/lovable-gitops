apiVersion: v1
kind: Service

metadata:
  name: lovable-frontend
  namespace: lovable-core

spec:
  type: ClusterIP

  selector:
    app: lovable-frontend

  ports:
    - port: 80
      targetPort: 80