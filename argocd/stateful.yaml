apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: lovable-stateful
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/ISHADOW007/lovable-gitops.git
    targetRevision: main
    path: stateful

  destination:
    server: https://kubernetes.default.svc
    namespace: lovable-core

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true